<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >

<TextView
    android:id="@+id/recent_search"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintTop_toTopOf="parent"
    tools:text="Ricerche recenti"
    android:name="search_recent_margin_top"
    android:layout_marginTop="@dimen/bottom_bar_text_size"
    android:textColor="@color/selected_button"
    android:textStyle="bold"
    android:layout_marginStart="@dimen/bottom_bar_text_size"
    />

</FrameLayout>